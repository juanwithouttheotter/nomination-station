import React from 'react';
import './ContainerItem.css';

function ContainerItem(props) {
    <div className={(`.order-${props.order}`), props.flex, props.align-self}></div>
}

export default ContainerItem;